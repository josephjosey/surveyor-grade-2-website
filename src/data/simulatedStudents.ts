import { User, MockTestAttempt } from '../types';

// All dummy candidates and simulated attempts have been purged per user request.
// Only real student accounts and authentic test submissions are retained across the platform.
export const SIMULATED_STUDENTS: User[] = [];
export const SIMULATED_ATTEMPTS: MockTestAttempt[] = [];
